<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Profile Page</title>
    <% include partials/head %>
  </head>
  <body>
    <div class="container">
      <header class="text-center">
        <h1>Profile Page</h1>
        <a href="/logout" class="btn btn-light btn-sm">
          Logout
        </a>
      </header>
      <hr>
      <div class="container">
        <div class="col-sm-6">
          <div class="card">
            <div class="card-body">
              <h3 class="card-title"><span class="fa fa-user"></span>Your Coutloot profile</h3>
              <p>
                <strong>Name</strong> : <%= user.name %> <br>
                <strong>email</strong> : <%= user.local.email %> <br>
                <img style= "width: 300px; height: 350px" src="<%= user.image %>" alt="image"/>
              </p>
              <a href="/logout" class="btn btn-light btn-sm">
                Logout
              </a>
              <a href="/profile/edit" class="btn btn-light btn-sm">
                Edit
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
